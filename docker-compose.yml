services:
  openclaw-gateway:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT:-18789}:18789"
      - "${OPENCLAW_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${OPENCLAW_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]

  openclaw-gateway-2:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN_2:-openclaw-local-token-2}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR_2:-C:/Users/adeka/.openclaw2}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR_2:-C:/Users/adeka/.openclaw2/workspace}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT_2:-18891}:18891"
      - "${OPENCLAW_BRIDGE_PORT_2:-18892}:18892"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${OPENCLAW_GATEWAY_BIND:-lan}",
        "--port",
        "18891",
      ]

  openclaw-gateway-3:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN_3}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR_3}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR_3}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT_3}:${OPENCLAW_GATEWAY_PORT_3}"
      - "${OPENCLAW_BRIDGE_PORT_3}:${OPENCLAW_BRIDGE_PORT_3}"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "lan",
        "--port",
        "18893",
      ]

  openclaw-gateway-4:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN_4}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR_4}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR_4}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT_4}:${OPENCLAW_GATEWAY_PORT_4}"
      - "${OPENCLAW_BRIDGE_PORT_4}:${OPENCLAW_BRIDGE_PORT_4}"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "lan",
        "--port",
        "18895",
      ]

  openclaw-gateway-5:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN_5}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR_5}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR_5}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT_5}:${OPENCLAW_GATEWAY_PORT_5}"
      - "${OPENCLAW_BRIDGE_PORT_5}:${OPENCLAW_BRIDGE_PORT_5}"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "lan",
        "--port",
        "18897",
      ]

  openclaw-gateway-6:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN_6}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR_6}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR_6}:/home/node/.openclaw/workspace
    ports:
      - "${OPENCLAW_GATEWAY_PORT_6}:${OPENCLAW_GATEWAY_PORT_6}"
      - "${OPENCLAW_BRIDGE_PORT_6}:${OPENCLAW_BRIDGE_PORT_6}"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "lan",
        "--port",
        "18899",
      ]

  openclaw-cli:
    image: ${OPENCLAW_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
      ZAI_API_KEY: ${ZAI_API_KEY}
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
